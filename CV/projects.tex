% =============================================================================
% CV/projects.tex
%
% Loads project entries in order defined by \projectorder.
% Override in job_details.tex to reorder or surface specific projects, e.g.,:
%   \def\projectorder{fsae,vex,cnc,intake}
%
% Available entries:
%   vex, fsae, cnc, intake
%
% Note: intake is wrapped in \showfor{isMech} inside its own file —
% it will silently produce nothing on non-mech builds regardless of ordering.
% =============================================================================

\ifdefined\projectorder\else
  \def\projectorder{vex,intake,fsae,cnc}
\fi

\cvsection{Relevant Projects}
\begin{cventries}
  \makeatletter
  \@for\entry:=\projectorder\do{\input{CV/projects/\entry.tex}}
  \makeatother
\end{cventries}
